
- Assisted Puput over Slack migrating Hermes from legacy Azure Foundry configuration to Erudifi LiteLLM. Puput accepted LiteLLM dashboard invite, was told self-service virtual-key creation is allowed, and created/saved a LiteLLM key in `~/.hermes/.env` on macOS; advised `chmod 600 ~/.hermes/.env` (they initially omitted the required space).
- Puput shared screenshots that exposed an existing Azure Foundry API key. Advised treating it as compromised and asking Infra to revoke/rotate it immediately; they should not share further config/key screenshots.
- Current Hermes config visible at `/Users/puputym/.hermes/config.yaml`: legacy `model` block still points to `azure-foundry`, `providers: {}`. Last guidance was to replace `providers: {}` with an Erudifi LiteLLM provider block and test with a `hermes chat --provider custom:erudifi-litellm ...` command. This configuration syntax was given without validating against the actual linked mono-infra guide; if continuing, retrieve and verify the guide/Hermes schema before recommending further changes.
