# 2026-04-17

- Ashil asked that whenever I create a skill, I should also mirror it into the workspace as a durable backup/reference copy.
- Preferred workspace mirror convention for created skills: `skills/<skill-name>/SKILL.md`.
- Mirrored `create-branch` skill into `/home/jesse/.openclaw/workspace/skills/create-branch/SKILL.md` after installing it in the OpenClaw skills directory.
# 2026-04-17

- Ashil asked that whenever I create a skill, I should also mirror it into the workspace as a durable backup/reference copy.
- Preferred workspace mirror convention for created skills: `skills/<skill-name>/SKILL.md`.
- Mirrored `create-branch` skill into `/home/jesse/.openclaw/workspace/skills/create-branch/SKILL.md` after installing it in the OpenClaw skills directory.
- GitHub account for this machine is `ringo-ai-bot`; GitHub CLI auth is working and SSH auth is configured to use `~/.ssh/id_ed25519_ringo`.
- Preferred Git commit identity for this machine is `Ringo AI Bot <ringo@erudifi.com>`.
- OpenCode is installed locally at `~/.opencode`, config is at `~/.config/opencode/opencode.json`, and the binary is on PATH as `opencode`.
- OpenCode requires a PTY/TTY in this environment for `opencode run`; a reliable wrapper is `script -q -c '<command>' /dev/null`.
- Verified OpenCode PTY validation command returned `OPENCODE_OK`.
- Created and mirrored the `commit` skill exactly from the reference into both the live skills directory and `/home/jesse/.openclaw/workspace/skills/commit/SKILL.md`.
