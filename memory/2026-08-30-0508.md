# Session: 2026-08-30 05:08:39 UTC

- **Session Key**: agent:main:slack:direct:u04tars9eca
- **Session ID**: 1ce647ad-0c35-447d-bc3f-53f3b03cc65a
- **Source**: slack

## Conversation Summary

assistant: ✅ New session started.
user: Can you look into why your slack app homepage kept reverting to this:
```OpenClaw
Send a DM, mention OpenClaw in a channel, or use /openclaw to start a session.
This Home tab is safe to show to any workspace member who opens the app.```
eventho i already post new homepage via slack API like below:
```POST <https://slack.com/api/views.publish>
Content-type: application/json
Authorization: Bearer ***
{
 "user_id": "U04TARS9ECA",
 "view": {
 "type": "home",
 "callback_id": "ringo_epd_home_v1",
 "blocks": [
 {
 "type": "header",
 "text": {
 "type": "plain_text",
 "text": "Ringo · EPD Teams Assistant",
 "emoji": true
 }
 },
 {
 "type": "section",
 "text": {
 "type": "mrkdwn",
 "text": "I'm Erudifi's EPD teammate for product, operations, analytics, incident response, and technical discussion. I'm direct, evidence-led, and useful when something is on fire."
 }
 },
 {
 "type": "divider"
 },
 {
 "type": "section",
 "text": {
 "type": "mrkdwn",
 "text": "*What I can help with*\n• Investigate incidents, errors, and operational questions\n• Discuss product decisions, trade-offs, and delivery risks\n• Look up lending metrics and portfolio data\n• Inspect Airflow pipelines, DAG runs, and task logs\n• Review existing GitHub PRs and CI; discuss repositories and tickets\n• Retrieve authorized private core-loans documents\n\n*Boundary:* Clara owns implementation, commits, and pull requests. I'll help frame, investigate, and review the work."
 }
 },
 {
 "type": "divider"
 },
 {
 "type": "section",
 "text": {
 "type": "mrkdwn",
 "text": "*Useful session commands*\n`/new [model]` — Start a clean session\n`/reset` — Reset this session\n`/compact [instructions]` — Condense context while preserving what matters\n`/stop` — Stop the current response or task\n`/session idle &lt;duration|off&gt;` — Set how long this thread stays bound\n`/session max-age &lt;duration|off&gt;` — Set a maximum thread lifetime\n`/btw &lt;question&gt;` — Ask a side question without changing this conversation\n`/tasks` — See recent or active background work\n`/agentstatus` — See Ringo's runtime status\n`/help` — Show concise command help"
 }
 },
 {
 "type": "context",
 "elements": [
 {
 "type": "mrkdwn",
 "text": "Tip: You can also DM me normally, or mention me in an allowed channel. Commands must be registered in the Slack app manifest before they appear in Slack."
 }
 ]
 },
 {
 "type": "divider"
 },
 {
 "type": "section",
 "text": {
 "type": "mrkdwn",
 "text": "*Good starting prompts*\n• \"What failed in the latest pipeline run?\"\n• \"Pull the repayment trend for this month.\"\n• \"Summarize this incident and suggest next steps.\"\n• \"What does this repository / PR / ticket mean for EPD?\""
 }
 }
 ]
 }
}```
assistant: Found it. This isn’t Slack losing your `views.publish`; *OpenClaw is deliberately overwriting it* whenever the Home tab opens.

In the installed Slack plugin (`@openclaw/slack` 2026.7.1), OpenClaw subscribes to `app_home_opened` and immediately calls `views.publish` with its hard-coded default:

- callback ID: `openclaw:home`
- Header: `OpenClaw`
- The exact “Send a DM…” / “safe to show…” copy you’re seeing

There is no configured custom Home-view setting in the current Slack channel config. The local OpenClaw docs explicitly describe this as intended behavior: opening App Home publishes OpenClaw’s safe default view.

So the sequence is:

1. Your API request successfully publishes `ringo_epd_home_v1` for `U04TARS9ECA`.
2. Slack emits `app_home_opened` whenever the user opens or revisits Home.
3. OpenClaw receives it over Socket Mode and publishes its default `openclaw:home` view for that same user.
4. Your version is replaced.

Your JSON itself looks structurally fine. The issue is the competing publisher, not the Block Kit payload.

*Best fix:* remove `app_home_opened` from this app’s Slack Event Subscriptions / manifest. That stops Slack from delivering the event, so OpenClaw’s handler never runs and your publisher owns the Home tab. This only sacrifices OpenClaw’s stock Home-tab behavior; DMs, mentions, and slash commands remain unaffected.

I would *not* patch OpenClaw’s installed JS: upgrades will overwrite it and it creates an ugly maintenance trap. The proper long-term solution is an upstream OpenClaw config option such as `channels.slack.appHome.enabled: false` or a customizable Home-view provider.
assistant: ⚠️ 🛠️ Exec failed: `print text -> show last 100 lines (+3 steps) → run true` (agent)
